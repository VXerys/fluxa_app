assets — Static assets for the app

Folders:
- `icons/` — small icons used by UI
- `themes/` — theme images or JSON if any
- `fonts/` — custom fonts (e.g., Inter)

Guidelines:
- Keep filenames descriptive: `ic_add.png`, `ic_home.png`.
- Register fonts and assets in `pubspec.yaml` when added.