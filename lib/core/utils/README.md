core/utils — Utility helpers

Purpose:
- Place stateless helpers: `logger.dart`, `currency_formatter.dart`, `date_formatter.dart`.

Guidelines:
- Keep utilities pure and testable.
- Do not put business logic here; utilities only for formatting/logging.