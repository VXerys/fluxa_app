import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Global singletons can be registered here if needed in future.
    // For now, core services are initialized in `main()` before runApp.
  }
}
