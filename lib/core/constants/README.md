core/constants — design tokens & app constants

Purpose:
- Central place for design tokens (colors, spacing, typography).
- App-level constants: `appName`, `supabase` keys, free quotas.

Files to add later:
- `app_constants.dart`
- `app_colors.dart`
- `app_spacing.dart`
- `app_text_styles.dart`
- `default_categories.dart`

Notes:
- Keep values as constants and avoid hardcoding in widgets.