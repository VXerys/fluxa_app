core/routes — Route names and GetPage mapping

Purpose:
- `app_routes.dart` for route string constants.
- `app_pages.dart` for `List<GetPage>` mapping and binding attachment.

Guidelines:
- Use consistent naming: `/main/home`, `/transactions/list`, `/transactions/add`, `/profile`.
- Keep route strings centralized to avoid typos.