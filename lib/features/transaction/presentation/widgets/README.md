presentation/widgets — Transaction-specific widgets

Examples:
- `transaction_item_widget.dart` — single row for list
- `transaction_form_field.dart` — numeric input for amount

Keep widgets small, stateless when possible, and theme-aware.