presentation/pages — Profile Page

Elements:
- App name & version
- Button: `Reset Data` -> confirms and clears StorageService & local DB
- If auth present: `Logout` button

Keep UI simple and confirm destructive actions.