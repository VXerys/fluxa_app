presentation/controllers — ProfileController

Responsibilities (optional):
- Handle reset data flow (confirmation dialog, call StorageService & DB helpers)
- Expose loading state

If not needed, implement reset directly in page using StorageService helpers.