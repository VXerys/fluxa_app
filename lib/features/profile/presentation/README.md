profile/presentation — Profile UI

Contains pages and optional controller for reset/logout logic.

Pages:
- `profile_page.dart` (app info + reset button)

Controller:
- `ProfileController` (optional) to handle reset flow and loading state.