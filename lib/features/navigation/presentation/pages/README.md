presentation/pages — MainNavigationPage

Layout:
- Scaffold with `BottomNavigationBar` and `IndexedStack` body
- FAB in center/right to open `AddTransactionPage`

Keep transitions simple; use GetX for navigation.