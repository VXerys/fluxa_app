features — Feature-first folders for MVP

Purpose:
- Each feature is self-contained: `data/`, `domain/`, `presentation/`.
- Keep feature code isolated and testable.

MVP features:
- `transaction` (core)
- `home` (summary)
- `profile` (settings)
- `navigation` (main shell)

Follow Clean Architecture contracts when implementing.