presentation/bindings — HomeBinding

Register `GetHomeSummaryUseCase` and `HomeController` here.

If `GetHomeSummaryUseCase` depends on TransactionRepository, ensure repository is available globally or via TransactionBinding.