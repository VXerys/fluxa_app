home/presentation — Home UI

Contains binding, controller, page, and widgets for summary and recent list.

Controller responsibilities:
- call `GetHomeSummaryUseCase` on init
- expose totals and recent transactions via Rx